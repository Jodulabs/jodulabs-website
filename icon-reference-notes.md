# Icon reference notes

The attached icon-design task identifies **Infinite Dwellings** as the clearest direction: two home-like forms become an uninterrupted infinity path. The referenced production candidate is `jodulabs_icon_48px.png`, a compact cyan/aqua infinity mark on a dark field. The website currently uses a temporary two-diamond monogram in the header/footer, so the replacement should use the infinite-symbol mark consistently and include a clean favicon treatment.
